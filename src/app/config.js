// Конфигурационный файл для хранения настроек приложения

const config = {
  // Базовый URL для API запросов через прокси Next.js (для обхода CORS)
  apiBaseUrl: '/api',
  
  // Таймаут для API запросов (в миллисекундах)
  apiTimeout: 30000,
  
  // Версия приложения
  appVersion: '1.0.0',
};

export default config; 